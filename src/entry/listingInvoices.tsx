import * as React from "react";
import * as ReactDOM from "react-dom";
import {ListingInvoices} from "../components/listingInvoices";

const rootEl = document.getElementById('app-root');

ReactDOM.render(<ListingInvoices/>, rootEl);
